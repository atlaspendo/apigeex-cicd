name: Apigee Proxy Deployment [Reusable Workflow]

on:
  workflow_call:
    inputs:
      proxy_name:
        description: The name of the API proxy to deploy
        required: true
        type: string
      proxy_directory:
        description: Working directory to zip up as your proxy bundle
        required: false
        type: string
        default: 'apiproxy'
      environment_group:
        description: The environment group to deploy to
        required: true
        type: string
      environment_type:
        description: The environment types to deploy to
        required: true
        type: string
      runner:
        description: The runner to use for the job
        required: false
        default: 'ubuntu-latest'
        type: string
    secrets:
      apigee_org:
        required: true
      workload_identity_provider:
        required: true
      service_account:
        required: true

permissions:
  contents: read
  id-token: write

jobs:
  Prepare_Environment_List:
    runs-on: ${{ inputs.runner }}
    outputs:
      environments: ${{ steps.set-envs.outputs.environments }}
    steps:
      - name: Set Environment List
        id: set-envs
        run: |
          GROUP="${{ inputs.environment_group }}"
          TYPES="${{ inputs.environment_type }}"
          
          generate_env_list() {
            local group=$1
            local types=$2
            local envs="["
            
            IFS=',' read -ra ENV_TYPES <<< "$types"
            local first=true
            
            for type in "${ENV_TYPES[@]}"; do
              if [ "$first" = true ]; then
                first=false
              else
                envs="${envs},"
              fi
              
              if [ "$group" = "default" ]; then
                envs="${envs}\"${type}\""
              else
                envs="${envs}\"${group}-${type}\""
              fi
            done
            
            envs="${envs}]"
            echo "$envs"
          }
          
          ENVIRONMENTS=$(generate_env_list "$GROUP" "$TYPES")
          echo "environments=$ENVIRONMENTS" >> $GITHUB_OUTPUT

  GCP_Auth:
    needs: [Prepare_Environment_List]
    runs-on: ${{ inputs.runner }}
    outputs:
      access_token: ${{ steps.auth.outputs.access_token }}
    steps:
      - name: Authenticate to Google Cloud
        id: auth
        uses: 'google-github-actions/auth@v1'
        with:
          workload_identity_provider: ${{ secrets.workload_identity_provider }}
          service_account: ${{ secrets.service_account }}
          token_format: 'access_token'

  ApigeeLint:
    needs: [GCP_Auth]
    runs-on: ${{ inputs.runner }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20'

      - name: Install apigeelint
        run: npm install -g apigeelint

      - name: Run apigeelint
        run: apigeelint -s ${{ inputs.proxy_directory }} -f table.js

  Prepare_and_Upload:
    needs: [GCP_Auth, ApigeeLint]
    runs-on: ${{ inputs.runner }}
    outputs:
      latest_revision: ${{ steps.upload.outputs.revision }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Authenticate to Google Cloud
        id: auth
        uses: 'google-github-actions/auth@v1'
        with:
          workload_identity_provider: ${{ secrets.workload_identity_provider }}
          service_account: ${{ secrets.service_account }}
          token_format: 'access_token'

      - name: Install apigeecli
        run: |
          mkdir -p $HOME/.apigeecli/bin
          curl -L https://raw.githubusercontent.com/apigee/apigeecli/main/downloadLatest.sh | sh -
          echo "$HOME/.apigeecli/bin" >> $GITHUB_PATH
          $HOME/.apigeecli/bin/apigeecli --help

      - name: Create API bundle
        id: create-bundle
        working-directory: ${{ github.workspace }}
        run: |
          create_bundle() {
            local source_dir="$1"
            echo "Creating bundle from: $source_dir"
            mkdir -p temp_bundle
            cp -r "$source_dir" temp_bundle/apiproxy
            cd temp_bundle
            zip -r ../proxy.zip apiproxy
            cd ..
            echo "Zip contents:"
            unzip -l proxy.zip
            rm -rf temp_bundle
          }
          
          if [ -d "${{ inputs.proxy_directory }}" ]; then
            create_bundle "${{ inputs.proxy_directory }}"
          elif [ -d "src/${{ inputs.proxy_directory }}" ]; then
            create_bundle "src/${{ inputs.proxy_directory }}"
          else
            echo "Error: Could not find apiproxy directory"
            exit 1
          fi

      - name: Upload and Get Revision
        id: upload
        env:
          APIGEE_ORG: ${{ secrets.apigee_org }}
          PROXY_NAME: ${{ inputs.proxy_name }}
          ACCESS_TOKEN: ${{ steps.auth.outputs.access_token }}
        run: |
          echo "Importing API proxy bundle to Apigee organization"
          IMPORT_OUTPUT=$($HOME/.apigeecli/bin/apigeecli apis create bundle -n "$PROXY_NAME" -p proxy.zip \
            --org "$APIGEE_ORG" \
            --token "$ACCESS_TOKEN")
          
          echo "$IMPORT_OUTPUT"
          
          LATEST_REVISION=$(echo "$IMPORT_OUTPUT" | grep -oP '"revision": "\K[^"]+')
          
          if [ -z "$LATEST_REVISION" ]; then
            echo "Failed to get revision number"
            exit 1
          fi
          
          echo "Latest revision: $LATEST_REVISION"
          echo "revision=$LATEST_REVISION" >> $GITHUB_OUTPUT

  Deploy_to_Environment:
    needs: [Prepare_Environment_List, GCP_Auth, Prepare_and_Upload]
    strategy:
      matrix:
        environment: ${{ fromJSON(needs.Prepare_Environment_List.outputs.environments) }}
      fail-fast: false
      max-parallel: 1
    runs-on: ${{ inputs.runner }}
    environment: ${{ matrix.environment }}
    steps:
      - name: Authenticate to Google Cloud
        id: auth
        uses: 'google-github-actions/auth@v1'
        with:
          workload_identity_provider: ${{ secrets.workload_identity_provider }}
          service_account: ${{ secrets.service_account }}
          token_format: 'access_token'

      - name: Install apigeecli
        run: |
          mkdir -p $HOME/.apigeecli/bin
          curl -L https://raw.githubusercontent.com/apigee/apigeecli/main/downloadLatest.sh | sh -
          echo "$HOME/.apigeecli/bin" >> $GITHUB_PATH

      - name: Deploy to Environment
        env:
          APIGEE_ORG: ${{ secrets.apigee_org }}
          APIGEE_ENV: ${{ matrix.environment }}
          PROXY_NAME: ${{ inputs.proxy_name }}
          ACCESS_TOKEN: ${{ steps.auth.outputs.access_token }}
          LATEST_REVISION: ${{ needs.Prepare_and_Upload.outputs.latest_revision }}
        run: |
          if [ -z "$LATEST_REVISION" ]; then
            echo "Error: LATEST_REVISION is empty. Deployment cannot proceed."
            exit 1
          fi
          
          echo "Deploying proxy '$PROXY_NAME' revision '$LATEST_REVISION' to environment '$APIGEE_ENV'"
          
          $HOME/.apigeecli/bin/apigeecli apis deploy \
            --name "$PROXY_NAME" \
            --org "$APIGEE_ORG" \
            --env "$APIGEE_ENV" \
            --rev "$LATEST_REVISION" \
            --token "$ACCESS_TOKEN" \
            --ovr \
            --wait

  Cleanup_Old_Revisions:
    needs: [Deploy_to_Environment]
    if: always()
    runs-on: ${{ inputs.runner }}
    steps:
      - name: Authenticate to Google Cloud
        id: auth
        uses: 'google-github-actions/auth@v1'
        with:
          workload_identity_provider: ${{ secrets.workload_identity_provider }}
          service_account: ${{ secrets.service_account }}
          token_format: 'access_token'

      - name: Delete Older Revisions
        env:
          APIGEE_ORG: ${{ secrets.apigee_org }}
          APIGEE_PROXY_NAME: ${{ inputs.proxy_name }}
          ACCESS_TOKEN: ${{ steps.auth.outputs.access_token }}
        run: |
          REVISIONS=$(curl -s -H "Authorization: Bearer $ACCESS_TOKEN" \
            "https://apigee.googleapis.com/v1/organizations/$APIGEE_ORG/apis/$APIGEE_PROXY_NAME/revisions")
          
          readarray -t REVISIONS < <(echo "$REVISIONS" | jq -r '.[]' | sort -n)
          TOTAL_REVISIONS=${#REVISIONS[@]}
          KEEP_COUNT=5
          
          if [ "$TOTAL_REVISIONS" -gt "$KEEP_COUNT" ]; then
            DELETE_COUNT=$((TOTAL_REVISIONS - KEEP_COUNT))
            for ((i=0; i<DELETE_COUNT; i++)); do
              REV="${REVISIONS[$i]}"
              curl -X DELETE -H "Authorization: Bearer $ACCESS_TOKEN" \
                "https://apigee.googleapis.com/v1/organizations/$APIGEE_ORG/apis/$APIGEE_PROXY_NAME/revisions/$REV"
            done
          fi

  ```yaml
  Deployment_Summary:
    needs: [ApigeeLint, Prepare_and_Upload, Deploy_to_Environment, Prepare_Environment_List, Cleanup_Old_Revisions, GCP_Auth]
    if: always()
    runs-on: ${{ inputs.runner }}
    steps:
      - name: Generate Enhanced Summary
        run: |
          TIMESTAMP=$(date '+%d/%m/%Y, %H:%M:%S')
          
          cat << EOF >> $GITHUB_STEP_SUMMARY
          <div align="center">
          
          # 🎯 Apigee Proxy Deployment Dashboard
          
          ### ⏱️ Deployment Time: $TIMESTAMP
          
          </div>
          
          ## 📊 Deployment Pipeline Flow

          \`\`\`mermaid
          graph LR
              A[Authentication] -->|${{ needs.GCP_Auth.result }}| B[Linting]
              B -->|${{ needs.ApigeeLint.result }}| C[Upload]
              C -->|${{ needs.Prepare_and_Upload.result }}| D[Deployment]
              D -->|${{ needs.Deploy_to_Environment.result }}| E[Cleanup]
              
              style A fill:#0366d6,color:#fff
              style B fill:#28a745,color:#fff
              style C fill:#6f42c1,color:#fff
              style D fill:#d73a49,color:#fff
              style E fill:#1b1f23,color:#fff
          \`\`\`

          ## 🎯 Deployment Status Chart

          \`\`\`mermaid
          pie title Deployment Stage Status
              "Successful" : $([ "${{ needs.Deploy_to_Environment.result }}" == "success" ] && \
                             [ "${{ needs.GCP_Auth.result }}" == "success" ] && \
                             [ "${{ needs.ApigeeLint.result }}" == "success" ] && \
                             [ "${{ needs.Prepare_and_Upload.result }}" == "success" ] && echo "100" || echo "0")
              "Failed" : $([ "${{ needs.Deploy_to_Environment.result }}" == "success" ] && \
                          [ "${{ needs.GCP_Auth.result }}" == "success" ] && \
                          [ "${{ needs.ApigeeLint.result }}" == "success" ] && \
                          [ "${{ needs.Prepare_and_Upload.result }}" == "success" ] && echo "0" || echo "100")
          \`\`\`

          ## 🌐 Environment Deployment Timeline

          \`\`\`mermaid
          gantt
              title Environment Deployment Timeline
              dateFormat HH:mm:ss
              axisFormat %H:%M:%S
              
              section Pipeline
              Authentication :done, auth, $(date '+%H:%M:%S'), 2m
              Linting      :done, lint, after auth, 2m
              Upload      :done, upload, after lint, 2m
              $(for env in $(echo ${{ inputs.environment_type }} | tr ',' ' '); do
                if [ "${{ inputs.environment_group }}" == "default" ]; then
                  ENV_NAME="${env}"
                else
                  ENV_NAME="${{ inputs.environment_group }}-${env}"
                fi
                echo "Deploy to ${ENV_NAME} :done, dep_${env}, after upload, 3m"
              done)
          \`\`\`

          ## 📈 Stage Details

          <table align="center">
            <tr>
              <td align="center">
                <h3>📦 PROXY</h3>
                <code style="background-color: #0366d6; color: white; padding: 4px 8px; border-radius: 4px;">
                  ${{ inputs.proxy_name }}
                </code>
              </td>
              <td align="center">
                <h3>🔢 VERSION</h3>
                <code style="background-color: #28a745; color: white; padding: 4px 8px; border-radius: 4px;">
                  ${{ needs.Prepare_and_Upload.outputs.latest_revision }}
                </code>
              </td>
              <td align="center">
                <h3>🔍 GROUP</h3>
                <code style="background-color: #6f42c1; color: white; padding: 4px 8px; border-radius: 4px;">
                  ${{ inputs.environment_group }}
                </code>
              </td>
            </tr>
          </table>

          ## 🎯 Environment Status Matrix

          <table align="center">
            <tr>
              <th align="center">Environment</th>
              <th align="center">Status</th>
              <th align="center">Health</th>
            </tr>
            $(for env in $(echo ${{ inputs.environment_type }} | tr ',' ' '); do
              if [ "${{ inputs.environment_group }}" == "default" ]; then
                ENV_NAME="${env}"
              else
                ENV_NAME="${{ inputs.environment_group }}-${env}"
              fi
              if [ "${{ needs.Deploy_to_Environment.result }}" == "success" ]; then
                echo "<tr>"
                echo "<td align=\"center\"><code style=\"background-color: #2f363d; color: white; padding: 4px 8px; border-radius: 4px;\">${ENV_NAME}</code></td>"
                echo "<td align=\"center\"><span style=\"color: #28a745; font-weight: bold;\">✅ DEPLOYED</span></td>"
                echo "<td align=\"center\"><span style=\"background-color: #28a745; color: white; padding: 4px 8px; border-radius: 4px;\">OPTIMAL</span></td>"
                echo "</tr>"
              else
                echo "<tr>"
                echo "<td align=\"center\"><code style=\"background-color: #2f363d; color: white; padding: 4px 8px; border-radius: 4px;\">${ENV_NAME}</code></td>"
                echo "<td align=\"center\"><span style=\"color: #cb2431; font-weight: bold;\">❌ FAILED</span></td>"
                echo "<td align=\"center\"><span style=\"background-color: #cb2431; color: white; padding: 4px 8px; border-radius: 4px;\">CRITICAL</span></td>"
                echo "</tr>"
              fi
            done)
          </table>

          ## 🔍 Pipeline Status Chart

          \`\`\`mermaid
          stateDiagram-v2
              [*] --> Authentication
              Authentication --> Linting: ${{ needs.GCP_Auth.result }}
              Linting --> Upload: ${{ needs.ApigeeLint.result }}
              Upload --> Deployment: ${{ needs.Prepare_and_Upload.result }}
              Deployment --> [*]: ${{ needs.Deploy_to_Environment.result }}
          \`\`\`

          ## 📊 Pipeline Stage Results

          <table align="center">
            <tr>
              <th align="center">Stage</th>
              <th align="center">Status</th>
              <th align="center">Details</th>
            </tr>
            <tr>
              <td align="center">🔐 Authentication</td>
              <td align="center">$([ "${{ needs.GCP_Auth.result }}" == "success" ] && echo "<span style=\"color: #28a745; font-weight: bold;\">✅ SUCCESS</span>" || echo "<span style=\"color: #cb2431; font-weight: bold;\">❌ FAILED</span>")</td>
              <td align="center"><code style="background-color: #2f363d; color: white; padding: 4px 8px; border-radius: 4px;">Workload Identity</code></td>
            </tr>
            <tr>
              <td align="center">🔍 Linting</td>
              <td align="center">$([ "${{ needs.ApigeeLint.result }}" == "success" ] && echo "<span style=\"color: #28a745; font-weight: bold;\">✅ SUCCESS</span>" || echo "<span style=\"color: #cb2431; font-weight: bold;\">❌ FAILED</span>")</td>
              <td align="center"><code style="background-color: #2f363d; color: white; padding: 4px 8px; border-radius: 4px;">API Validation</code></td>
            </tr>
            <tr>
              <td align="center">📦 Upload</td>
              <td align="center">$([ "${{ needs.Prepare_and_Upload.result }}" == "success" ] && echo "<span style=\"color: #28a745; font-weight: bold;\">✅ SUCCESS</span>" || echo "<span style=\"color: #cb2431; font-weight: bold;\">❌ FAILED</span>")</td>
              <td align="center"><code style="background-color: #2f363d; color: white; padding: 4px 8px; border-radius: 4px;">Rev: ${{ needs.Prepare_and_Upload.outputs.latest_revision }}</code></td>
            </tr>
            <tr>
              <td align="center">🚀 Deployment</td>
              <td align="center">$([ "${{ needs.Deploy_to_Environment.result }}" == "success" ] && echo "<span style=\"color: #28a745; font-weight: bold;\">✅ SUCCESS</span>" || echo "<span style=\"color: #cb2431; font-weight: bold;\">❌ FAILED</span>")</td>
              <td align="center"><code style="background-color: #2f363d; color: white; padding: 4px 8px; border-radius: 4px;">Environment Deploy</code></td>
            </tr>
          </table>

          ## 📈 Final Status

          <div align="center" style="margin: 20px 0;">
            $(if [ "${{ needs.Deploy_to_Environment.result }}" == "success" ] && \
               [ "${{ needs.GCP_Auth.result }}" == "success" ] && \
               [ "${{ needs.ApigeeLint.result }}" == "success" ] && \
               [ "${{ needs.Prepare_and_Upload.result }}" == "success" ]; then
              echo "<div style=\"background-color: #28a745; color: white; padding: 16px; border-radius: 8px; display: inline-block;\">"
              echo "<h3 style=\"margin: 0;\">✨ Deployment Successfully Completed</h3>"
              echo "</div>"
            else
              echo "<div style=\"background-color: #cb2431; color: white; padding: 16px; border-radius: 8px; display: inline-block;\">"
              echo "<h3 style=\"margin: 0;\">⚠️ Deployment Issues Detected</h3>"
              echo "</div>"
            fi)
          </div>

          ---

          <div align="center" style="margin-top: 20px;">
            <code style="background-color: #2f363d; color: white; padding: 4px 8px; border-radius: 4px;">
              Run ID: ${{ github.run_id }}
            </code>
          </div>
          EOF

