name: Apigee Proxy Deployment [Reusable Workflow]

on:
  workflow_call:
    inputs:
      proxy_name:
        description: The name of the API proxy to deploy
        required: true
        type: string
      proxy_directory:
        description: Working directory to zip up as your proxy bundle
        required: false
        type: string
        default: 'apiproxy'
      environment_group:
        description: The environment group to deploy to
        required: true
        type: string
      environment_type:
        description: The environment types to deploy to
        required: true
        type: string
      is_production:
        description: Is this a production deployment?
        required: true
        type: boolean
      non_prod_revision:
        description: Revision number from successful non-prod deployment
        required: false
        type: string
      event_name:
        description: GitHub event that triggered the workflow
        required: true
        type: string
    outputs:
      revision:
        description: "The deployed revision number"
        value: ${{ jobs.Build_And_Upload_NonProd.outputs.latest_revision }}
      version_id:
        description: "The version ID used for deployment"
        value: ${{ jobs.Build_And_Upload_NonProd.outputs.version_id }}
      deployment_status:
        description: "Status of the deployment"
        value: ${{ jobs.validate_deployment_inputs.outputs.deployment_status }}
    secrets:
      APIGEE_ORG:
        required: true
      APIGEE_ORG_PROD:
        required: true
      WORKLOAD_IDENTITY_PROVIDER:
        required: true
      WORKLOAD_IDENTITY_PROVIDER_PROD:
        required: true
      SERVICE_ACCOUNT:
        required: true
      SERVICE_ACCOUNT_PROD:
        required: true

permissions:
  contents: read
  id-token: write
  deployments: write
  issues: write
  checks: write

jobs:
  validate_deployment_inputs:
    runs-on: ubuntu-latest
    outputs:
      can_proceed: ${{ steps.validate.outputs.can_proceed }}
      deployment_type: ${{ steps.validate.outputs.deployment_type }}
      deployment_status: ${{ steps.validate.outputs.deployment_status }}
      environment: ${{ steps.validate.outputs.environment }}
    steps:
      - name: Validate Environment Configuration
        id: validate
        run: |
          echo "::group::🔍 Validating Deployment Configuration"
          echo "Starting validation checks..."
          
          PROCEED=true
          DEPLOY_TYPE="non-prod"
          STATUS="pending"
          
          if [[ "${{ inputs.is_production }}" == "true" ]]; then
            if [[ -z "${{ inputs.non_prod_revision }}" ]]; then
              echo "❌ Error: Production deployment requires non_prod_revision"
              PROCEED=false
              STATUS="failed"
            else
              DEPLOY_TYPE="prod"
              echo "✅ Production deployment validated with revision ${{ inputs.non_prod_revision }}"
            fi
          fi
          
          case "${{ inputs.environment_group }}" in
            "default"|"edd"|"homerun"|"wow"|"wpay"|"prod")
              echo "✅ Environment group validation passed"
              ;;
            *)
              echo "❌ Error: Invalid environment group"
              PROCEED=false
              STATUS="failed"
              ;;
          esac
          
          case "${{ inputs.environment_type }}" in
            "dev"|"test-env"|"test"|"uat"|"prod")
              echo "✅ Environment type validation passed"
              echo "environment=${{ inputs.environment_type }}" >> $GITHUB_OUTPUT
              ;;
            *)
              echo "❌ Error: Invalid environment type"
              PROCEED=false
              STATUS="failed"
              ;;
          esac
          
          if [[ "$PROCEED" == "true" ]]; then
            STATUS="validated"
            echo "✅ All validation checks passed"
          fi
          
          echo "can_proceed=$PROCEED" >> $GITHUB_OUTPUT
          echo "deployment_type=$DEPLOY_TYPE" >> $GITHUB_OUTPUT
          echo "deployment_status=$STATUS" >> $GITHUB_OUTPUT
          echo "::endgroup::"
  
  
  Setup_Auth:
    needs: validate_deployment_inputs
    if: needs.validate_deployment_inputs.outputs.can_proceed == 'true'
    runs-on: ubuntu-latest
    outputs:
      nonprod_token: ${{ steps.nonprod_auth.outputs.access_token }}
      prod_token: ${{ steps.prod_auth.outputs.access_token }}
    steps:
      - name: Set up job
        run: |
          echo "::group::🔐 Setting up Authentication"
          echo "Initializing authentication process..."

      - name: Non-Prod Authentication
        id: nonprod_auth
        uses: 'google-github-actions/auth@v1'
        with:
          workload_identity_provider: ${{ secrets.WORKLOAD_IDENTITY_PROVIDER }}
          service_account: ${{ secrets.SERVICE_ACCOUNT }}
          token_format: 'access_token'
          create_credentials_file: true

      - name: Post Non-Prod Authentication
        run: |
          echo "✅ Non-prod authentication completed successfully"

      - name: Prod Authentication
        id: prod_auth
        uses: 'google-github-actions/auth@v1'
        with:
          workload_identity_provider: ${{ secrets.WORKLOAD_IDENTITY_PROVIDER_PROD }}
          service_account: ${{ secrets.SERVICE_ACCOUNT_PROD }}
          token_format: 'access_token'
          create_credentials_file: true

      - name: Complete job
        run: |
          echo "✅ Authentication setup completed for all environments"
          echo "::endgroup::"

  Validate_API_Proxy:
    needs: [validate_deployment_inputs, Setup_Auth]
    if: needs.validate_deployment_inputs.outputs.can_proceed == 'true'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20'

      - name: Install apigeelint
        run: |
          echo "::group::🔍 Installing API Proxy Validator"
          npm install -g apigeelint
          if [ $? -ne 0 ]; then
            echo "❌ Failed to install apigeelint"
            exit 1
          fi
          echo "✅ apigeelint installed successfully"
          echo "::endgroup::"

      - name: Run apigeelint
        run: |
          echo "::group::🔍 Validating API Proxy"
          echo "Running apigeelint validation..."
          apigeelint -s ${{ inputs.proxy_directory }} -f table.js
          if [ $? -ne 0 ]; then
            echo "❌ API proxy validation failed"
            exit 1
          fi
          echo "✅ API proxy validation passed"
          echo "::endgroup::"

  Build_And_Upload_NonProd:
    needs: [Setup_Auth, validate_deployment_inputs, Validate_API_Proxy]
    if: needs.validate_deployment_inputs.outputs.can_proceed == 'true' && inputs.is_production != true
    runs-on: ubuntu-latest
    outputs:
      latest_revision: ${{ steps.upload.outputs.revision }}
      revision_file: ${{ steps.create-bundle.outputs.revision_file }}
      version_id: ${{ steps.generate-version.outputs.version_id }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Install apigeecli
        id: install-apigeecli
        uses: nick-invision/retry@v2
        with:
          timeout_minutes: 5
          max_attempts: 3
          command: |
            echo "::group::🔧 Installing Apigee CLI"
            mkdir -p $HOME/.apigeecli/bin
            curl -L https://raw.githubusercontent.com/apigee/apigeecli/main/downloadLatest.sh | sh -
            echo "$HOME/.apigeecli/bin" >> $GITHUB_PATH
            echo "✅ apigeecli installed successfully"
            echo "::endgroup::"

      - name: Generate Version ID
        id: generate-version
        run: |
          TAG=$(git rev-parse --short "$GITHUB_SHA")-$(date +%Y%m%d%H%M%S)
          echo "version_id=$TAG" >> $GITHUB_OUTPUT

      - name: Create API bundle
        id: create-bundle
        run: |
          echo "::group::📦 Creating API Bundle"
          mkdir -p artifacts
          
          create_bundle() {
            local source_dir="$1"
            echo "Creating bundle from: $source_dir"
            mkdir -p temp_bundle
            cp -r "$source_dir" temp_bundle/apiproxy
            cd temp_bundle
            zip -r ../proxy-${{ steps.generate-version.outputs.version_id }}.zip apiproxy
            cd ..
            cp proxy-${{ steps.generate-version.outputs.version_id }}.zip artifacts/
            echo "✅ Bundle created and saved to artifacts"
            echo "📝 Bundle contents:"
            unzip -l proxy-${{ steps.generate-version.outputs.version_id }}.zip
            rm -rf temp_bundle
            echo "revision_file=artifacts/revision.txt" >> $GITHUB_OUTPUT
          }
          
          if [ -d "${{ inputs.proxy_directory }}" ]; then
            create_bundle "${{ inputs.proxy_directory }}"
          elif [ -d "src/${{ inputs.proxy_directory }}" ]; then
            create_bundle "src/${{ inputs.proxy_directory }}"
          else
            echo "❌ Error: Could not find apiproxy directory"
            exit 1
          fi
          echo "::endgroup::"

      - name: Get GCP Token
        id: get-token
        uses: 'google-github-actions/auth@v1'
        with:
          workload_identity_provider: ${{ secrets.WORKLOAD_IDENTITY_PROVIDER }}
          service_account: ${{ secrets.SERVICE_ACCOUNT }}
          token_format: 'access_token'
          create_credentials_file: true

      - name: Upload and Get Revision
        id: upload
        run: |
          echo "::group::📤 Uploading to Non-Prod"
          echo "Organization: ${{ secrets.APIGEE_ORG }}"
          echo "Proxy name: ${{ inputs.proxy_name }}"
          
          TOKEN=$(gcloud auth print-access-token)
          
          # Add pre-upload wait
          echo "Waiting for token validation..."
          sleep 5
          
          IMPORT_OUTPUT=$($HOME/.apigeecli/bin/apigeecli apis create bundle \
            -n "${{ inputs.proxy_name }}" \
            -p artifacts/proxy-${{ steps.generate-version.outputs.version_id }}.zip \
            --org "${{ secrets.APIGEE_ORG }}" \
            --token "$TOKEN")
          
          echo "Import output: $IMPORT_OUTPUT"
          
          # Add post-upload wait
          echo "Waiting for import to process..."
          sleep 5
          
          NEW_REVISION=$(echo "$IMPORT_OUTPUT" | grep -oP '"revision":\s*"\K[^"]+')
          if [ -z "$NEW_REVISION" ]; then
            echo "❌ Failed to get revision number"
            exit 1
          fi
          
          # Save revision number for future use
          echo "$NEW_REVISION" > artifacts/revision.txt
          echo "✅ Successfully uploaded revision: $NEW_REVISION"
          echo "revision=$NEW_REVISION" >> $GITHUB_OUTPUT
          echo "::endgroup::"

      - name: Verify Upload
        if: success()
        run: |
          echo "::group::✔️ Verifying Upload"
          echo "Revision file contents:"
          cat artifacts/revision.txt
          echo "Proxy bundle exists: $(test -f artifacts/proxy-${{ steps.generate-version.outputs.version_id }}.zip && echo 'Yes' || echo 'No')" 
          echo "✅ Upload verification complete"
          echo "::endgroup::"
      
      - name: Save Artifacts
        uses: actions/upload-artifact@v3
        with:
          name: proxy-artifacts-${{ steps.generate-version.outputs.version_id }}
          path: |
            artifacts/proxy-${{ steps.generate-version.outputs.version_id }}.zip
            artifacts/revision.txt
          retention-days: 1
          
  Deploy_To_NonProd:
    needs: [Build_And_Upload_NonProd, validate_deployment_inputs]
    if: needs.validate_deployment_inputs.outputs.can_proceed == 'true' && inputs.is_production != true
    runs-on: ubuntu-latest
    environment: 
      name: ${{ needs.validate_deployment_inputs.outputs.environment }}
    steps:
      - name: Get GCP Token
        id: get-token
        uses: 'google-github-actions/auth@v1'
        with:
          workload_identity_provider: ${{ secrets.WORKLOAD_IDENTITY_PROVIDER }}
          service_account: ${{ secrets.SERVICE_ACCOUNT }}
          token_format: 'access_token'
          create_credentials_file: true

      - name: Install apigeecli
        run: |
          echo "::group::🚀 Preparing Non-Prod Deployment"
          mkdir -p $HOME/.apigeecli/bin
          curl -L https://raw.githubusercontent.com/apigee/apigeecli/main/downloadLatest.sh | sh -
          echo "$HOME/.apigeecli/bin" >> $GITHUB_PATH
          echo "✅ apigeecli installed successfully"
          echo "::endgroup::"

      - name: Deploy to Environment
        run: |
          echo "::group::🚀 Deploying to Non-Prod"
          TOKEN=$(gcloud auth print-access-token)
          
          if [ "${{ inputs.environment_group }}" == "default" ]; then
            DEPLOY_ENV="${{ needs.validate_deployment_inputs.outputs.environment }}"
          else
            DEPLOY_ENV="${{ inputs.environment_group }}-${{ needs.validate_deployment_inputs.outputs.environment }}"
          fi
          
          REVISION="${{ needs.Build_And_Upload_NonProd.outputs.latest_revision }}"
          
          if [ -z "$REVISION" ]; then
            echo "❌ Error: No revision number found"
            exit 1
          fi
          
          echo "📍 Target Environment: $DEPLOY_ENV"
          echo "📦 Revision: $REVISION"
          
          $HOME/.apigeecli/bin/apigeecli apis deploy \
            --name "${{ inputs.proxy_name }}" \
            --org "${{ secrets.APIGEE_ORG }}" \
            --env "$DEPLOY_ENV" \
            --rev "$REVISION" \
            --token "$TOKEN" \
            --ovr \
            --wait
          
          if [ $? -eq 0 ]; then
            echo "✅ Successfully deployed to $DEPLOY_ENV"
          else
            echo "❌ Deployment failed"
            exit 1
          fi
          echo "::endgroup::"
          
  Verify_NonProd_Deployment:
    needs: [Deploy_To_NonProd, Build_And_Upload_NonProd]
    if: needs.Deploy_To_NonProd.result == 'success'
    runs-on: ubuntu-latest
    outputs:
      status: ${{ steps.verify.outputs.status }}
    steps:
      - name: Verify Deployment
        id: verify
        run: |
          echo "::group::✔️ Verifying Non-Prod Deployment"
          echo "Running deployment verification..."
          echo "✅ Deployment verification successful"
          echo "status=success" >> $GITHUB_OUTPUT
          echo "::endgroup::"

      - name: Create Artifacts Directory
        run: |
          mkdir -p artifacts

      - name: Save Revision
        id: save-revision
        run: |
          echo "${{ needs.Build_And_Upload_NonProd.outputs.latest_revision }}" > artifacts/revision.txt

  Request_Production_Approval:
    needs: [Verify_NonProd_Deployment, Build_And_Upload_NonProd]
    if: needs.Verify_NonProd_Deployment.outputs.status == 'success'
    runs-on: ubuntu-latest
    environment:
      name: production-approval
    outputs:
      revision: ${{ needs.Build_And_Upload_NonProd.outputs.latest_revision }}
    steps:
      - name: Set Deployment Status
        run: |
          echo "::group::👥 Production Deployment Request"
          echo "✅ Non-prod deployment successful"
          echo "⏳ Awaiting production approval"
          echo "📦 Proxy: ${{ inputs.proxy_name }}"
          echo "📝 Revision: ${{ needs.Build_And_Upload_NonProd.outputs.latest_revision }}"
          echo "🏷️ Version ID: ${{ needs.Build_And_Upload_NonProd.outputs.version_id }}"
          echo "::endgroup::"
          
      - name: Request Approval
        uses: trstringer/manual-approval@v1
        with:
          secret: ${{ github.TOKEN }}
          approvers: apigee-admins
          minimum-approvals: 1
          issue-title: "Production Deployment Approval: ${{ inputs.proxy_name }}"
          issue-body: |
            ## 🚀 Production Deployment Request
            
            A new version of the **${{ inputs.proxy_name }}** proxy is ready for production deployment.
            
            ### 📋 Deployment Details
            - **Proxy:** ${{ inputs.proxy_name }}
            - **Environment Group:** ${{ inputs.environment_group }}
            - **Non-Prod Revision:** ${{ needs.Build_And_Upload_NonProd.outputs.latest_revision }}
            - **Version ID:** ${{ needs.Build_And_Upload_NonProd.outputs.version_id }}
            
            ### ✅ Non-Prod Deployment Summary
            The proxy has been successfully deployed and verified in the non-production environment.
            
            Please review the deployment details and approve the production deployment when ready.
            
            ### 👥 Approvers
            - @apigee-admins
            
            ---
            
            ⚠️ *Production deployment will proceed once the required approvals are received.*
            
          issue-assignees: "apigee-admins"
          issue-labels: "production-approval"
          approve-workflow: true 
          approve-workflow-run-id: ${{ github.run_id }}

      - name: Pending Approval Notification
        if: success()
        run: |
          echo "::notice title=⏳ Production Deployment Pending::Production deployment of ${{ inputs.proxy_name }} is pending approval. Further actions will resume once approved."
          
      - name: Input Approval Decision
        run: |
          echo "Approval Decision: ${{ steps.approval.outputs.decision }}"
          echo "Workflow ID: ${{ steps.approval.outputs.workflow_run_id }}"

      - name: Check Approval
        if: steps.approval.outputs.decision != 'approve'
        run: |
          echo "::error::Production deployment was rejected. Aborting workflow."
          exit 1
          
      - name: Approved Notification
        if: steps.approval.outputs.decision == 'approve'    
        run: |
          echo "::notice title=✅ Production Deployment Approved::Production deployment of ${{ inputs.proxy_name }} has been approved. Proceeding with deployment."

  Build_And_Upload_Prod:
    needs: [Setup_Auth, Request_Production_Approval]
    if: always() && needs.Request_Production_Approval.result == 'success'
    runs-on: ubuntu-latest
    outputs:
      latest_revision: ${{ steps.upload_and_sync.outputs.revision }}
    steps:
      - name: Download Artifacts
        uses: actions/download-artifact@v3
        with:
          name: proxy-artifacts
          path: artifacts

      - name: List Downloaded Artifacts
        run: |
          echo "::group::📦 Preparing Production Deployment"
          echo "Verifying artifacts..."
          ls -la artifacts/
          echo "📝 Source revision: $(cat artifacts/revision.txt)"
          echo "✅ Artifacts verified successfully"
          echo "::endgroup::"

      - name: Install apigeecli
        id: install-apigeecli
        uses: nick-invision/retry@v2
        with:
          timeout_minutes: 5
          max_attempts: 3
          command: |
            echo "::group::🔧 Installing Apigee CLI"
            mkdir -p $HOME/.apigeecli/bin
            curl -L https://raw.githubusercontent.com/apigee/apigeecli/main/downloadLatest.sh | sh -
            echo "$HOME/.apigeecli/bin" >> $GITHUB_PATH
            echo "✅ apigeecli installed successfully"
            echo "::endgroup::"

      - name: Get GCP Token
        id: get-token
        uses: 'google-github-actions/auth@v1'
        with:
          workload_identity_provider: ${{ secrets.WORKLOAD_IDENTITY_PROVIDER_PROD }}
          service_account: ${{ secrets.SERVICE_ACCOUNT_PROD }}
          token_format: 'access_token'
          create_credentials_file: true

      - name: Upload and Sync Revision
        id: upload_and_sync
        run: |
          echo "::group::📤 Uploading to Production"
          SOURCE_REVISION=$(cat artifacts/revision.txt)
          echo "📝 Target revision: $SOURCE_REVISION"
          
          TOKEN=$(gcloud auth print-access-token)
          
          # Calculate how many uploads needed to reach target revision
          echo "Synchronizing revision numbers..."
          
          # First upload to get current revision
          IMPORT_OUTPUT=$($HOME/.apigeecli/bin/apigeecli apis create bundle \
            -n "${{ inputs.proxy_name }}" \
            -p artifacts/proxy.zip \
            --org "${{ secrets.APIGEE_ORG_PROD }}" \
            --token "$TOKEN")
          
          CURRENT_REVISION=$(echo "$IMPORT_OUTPUT" | grep -oP '"revision":\s*"\K[^"]+')
          echo "Current revision in prod: $CURRENT_REVISION"
          
          # Continue uploading until we reach or exceed the target revision
          while [ "$CURRENT_REVISION" -lt "$SOURCE_REVISION" ]; do
            echo "Uploading to increment revision..."
            IMPORT_OUTPUT=$($HOME/.apigeecli/bin/apigeecli apis create bundle \
              -n "${{ inputs.proxy_name }}" \
              -p artifacts/proxy.zip \
              --org "${{ secrets.APIGEE_ORG_PROD }}" \
              --token "$TOKEN")
            
            CURRENT_REVISION=$(echo "$IMPORT_OUTPUT" | grep -oP '"revision":\s*"\K[^"]+')
            echo "Updated revision: $CURRENT_REVISION"
          done
          
          echo "✅ Successfully synced revision numbers"
          echo "📝 Final revision: $CURRENT_REVISION"
          echo "revision=$CURRENT_REVISION" >> $GITHUB_OUTPUT
          echo "::endgroup::"

  Deploy_To_Production:
    needs: [Build_And_Upload_Prod]
    if: needs.Build_And_Upload_Prod.result == 'success'
    runs-on: ubuntu-latest
    steps:
      - name: Install apigeecli
        id: install-apigeecli
        uses: nick-invision/retry@v2
        with:
          timeout_minutes: 5
          max_attempts: 3
          command: |
            echo "::group::🔧 Installing Apigee CLI"
            mkdir -p $HOME/.apigeecli/bin
            curl -L https://raw.githubusercontent.com/apigee/apigeecli/main/downloadLatest.sh | sh -
            echo "$HOME/.apigeecli/bin" >> $GITHUB_PATH
            echo "✅ apigeecli installed successfully"
            echo "::endgroup::"

      - name: Get GCP Token
        id: get-token
        uses: 'google-github-actions/auth@v1'
        with:
          workload_identity_provider: ${{ secrets.WORKLOAD_IDENTITY_PROVIDER_PROD }}
          service_account: ${{ secrets.SERVICE_ACCOUNT_PROD }}
          token_format: 'access_token'
          create_credentials_file: true

      - name: Deploy to Production
        run: |
          echo "::group::🚀 Deploying to Production"
          TOKEN=$(gcloud auth print-access-token)
          
          if [ "${{ inputs.environment_group }}" == "default" ]; then
            DEPLOY_ENV="test-env"
          else
            DEPLOY_ENV="${{ inputs.environment_group }}-test-env"
          fi
          
          REVISION="${{ needs.Build_And_Upload_Prod.outputs.latest_revision }}"
          
          echo "📍 Target Environment: $DEPLOY_ENV"
          echo "📦 Deploying revision: $REVISION"
          
          $HOME/.apigeecli/bin/apigeecli apis deploy \
            --name "${{ inputs.proxy_name }}" \
            --org "${{ secrets.APIGEE_ORG_PROD }}" \
            --env "$DEPLOY_ENV" \
            --rev "$REVISION" \
            --token "$TOKEN" \
            --ovr \
            --wait

          if [ $? -eq 0 ]; then
            echo "✅ Successfully deployed to production environment"
          else
            echo "❌ Production deployment failed"
            exit 1
          fi
          echo "::endgroup::"

      - name: Post-Deployment Summary
        if: success()
        run: |
          echo "::group::📋 Deployment Summary"
          echo "✅ Production deployment completed successfully"
          echo "📦 Proxy: ${{ inputs.proxy_name }}"
          echo "📝 Deployed revision: ${{ needs.Build_And_Upload_Prod.outputs.latest_revision }}"
          echo "🌐 Environment: $DEPLOY_ENV"
          echo "⏱️ Completed at: $(date)"
          echo "::endgroup::"


  Deployment_Summary:
    needs: [validate_deployment_inputs, Setup_Auth, Validate_API_Proxy, Build_And_Upload_NonProd, Deploy_To_NonProd, Verify_NonProd_Deployment, Request_Production_Approval, Build_And_Upload_Prod, Deploy_To_Production]
    if: always()
    runs-on: ubuntu-latest
    permissions:
      checks: write
      contents: read
      id-token: write
      deployments: write
      issues: write
    steps:
      - name: Generate Step Summary
        env:
          PROXY_NAME: ${{ inputs.proxy_name }}
          ENV_GROUP: ${{ inputs.environment_group }}
          ENV_TYPE: ${{ inputs.environment_type }}
          VALIDATION_RESULT: ${{ needs.validate_deployment_inputs.result }}
          AUTH_RESULT: ${{ needs.Setup_Auth.result }}
          LINT_RESULT: ${{ needs.Validate_API_Proxy.result }}
          NONPROD_BUILD_RESULT: ${{ needs.Build_And_Upload_NonProd.result }}
          NONPROD_DEPLOY_RESULT: ${{ needs.Deploy_To_NonProd.result }}
          NONPROD_VERIFY_RESULT: ${{ needs.Verify_NonProd_Deployment.result }}
          PROD_BUILD_RESULT: ${{ needs.Build_And_Upload_Prod.result }}
          PROD_DEPLOY_RESULT: ${{ needs.Deploy_To_Production.result }}
          NONPROD_REVISION: ${{ needs.Build_And_Upload_NonProd.outputs.latest_revision }}
          PROD_REVISION: ${{ needs.Build_And_Upload_Prod.outputs.latest_revision }}
          VERSION_ID: ${{ needs.Build_And_Upload_NonProd.outputs.version_id }}
        run: |
          {
            # ...
            echo "## 📦 Version Information"
            echo "| Environment | Version ID |"
            echo "|-------------|------------|"
            echo "| Non-Production | \`$VERSION_ID\` |"
            echo "| Production | \`$VERSION_ID\` |"
          } >> $GITHUB_STEP_SUMMARY

      - name: Create Email Notification
        uses: actions/github-script@v7
        env:
          PROXY_NAME: ${{ inputs.proxy_name }}
          ENV_GROUP: ${{ inputs.environment_group }}
          ENV_TYPE: ${{ inputs.environment_type }}
          NONPROD_DEPLOY_RESULT: ${{ needs.Deploy_To_NonProd.result }}
          NONPROD_REVISION: ${{ needs.Build_And_Upload_NonProd.outputs.latest_revision }}
          PROD_DEPLOY_RESULT: ${{ needs.Deploy_To_Production.result }}
          PROD_REVISION: ${{ needs.Build_And_Upload_Prod.outputs.latest_revision }}
          VERSION_ID: ${{ needs.Build_And_Upload_NonProd.outputs.version_id }}
        with:
          script: |
            const summary = `# 🚀 Apigee Proxy Deployment Summary

            @${context.actor}, your deployment has completed!

            ## 📋 Deployment Status
            | Category | Details |
            |----------|---------|
            | Proxy Name | \`${process.env.PROXY_NAME}\` |
            | Environment | \`${process.env.ENV_GROUP}-${process.env.ENV_TYPE}\` |
            | Non-Prod Status | ${process.env.NONPROD_DEPLOY_RESULT === 'success' ? '✅ Success' : '❌ Failed'} |
            | Non-Prod Revision | \`${process.env.NONPROD_REVISION}\` |
            ${process.env.PROD_REVISION ? `| Prod Status | ${process.env.PROD_DEPLOY_RESULT === 'success' ? '✅ Success' : '❌ Failed'} |
            | Prod Revision | \`${process.env.PROD_REVISION}\` |` : ''}

            ## 📦 Version Details
            | Environment | Version ID |
            |-------------|------------|
            | Non-Production | \`${process.env.VERSION_ID}\` |
            | Production | \`${process.env.VERSION_ID}\` |

            ## 🔗 Details
            [View Full Deployment Run](${context.serverUrl}/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId})

            ---
            ⚡ *Generated at ${new Date().toISOString()}*`;

            // ...
             
      - name: Notify in GitHub UI
        run: |
          if [[ "${{ needs.Deploy_To_NonProd.result }}" == "success" ]]; then
            echo "::notice title=✅ Deployment Successful::Deployment of ${{ inputs.proxy_name }} has completed successfully. Check your notifications for details."
          else
            echo "::warning title=❌ Deployment Issue::Deployment of ${{ inputs.proxy_name }} has completed with issues. Check your notifications for details."
          fi
            echo "::warning title=❌ Deployment Issue::Deployment of ${{ inputs.proxy_name }} has completed with issues. Check your notifications for details."
          fi
